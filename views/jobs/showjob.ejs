<% include ../partials/header %>
<div class="container">
    <a id="profiles-back-btn" class="btn btn-sm btn-default" href="/jobs">Back to Job List</a>
    <div class="row">
        <div class="col-md-9">
            <h2>Dont you want to be the</h2>
            <h1 class="show-name"><%= jobs.jobRole %> @ <strong><%= jobs.jobVenueName %></strong>?</h1>
            <br>
            <div class="thumbnail">
                <div class="caption-full">
                    <strong><p>Job description</p></strong>
                    <p><%= jobs.jobDesc %></p>
                </div>
                <div class="caption-full">
                    <strong><p>Start date</p></strong>
                    <p>10/02/2018</p>
                </div>
                <div class="caption-full">
                    <strong><p>Interested? Contact</p></strong>
                    <p>Liam on 040000000</p>
                    <p>email: <a href="mailto:liam@mtphq.com">liam@mtphq.com</a></p>
                </div>     
                <div>                                    
                    <div class="caption-full">
                        <p>posted by <%= jobs.jobAuthor.username %> on <%= jobs.date %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--<div class="row">
        <div class="col-md-9">
            <div class="well">
                        <div class="text-right">
                                <a class="btn btn-mtp-black" href="/jobs/<%= jobs._id %>/comments/new">Add a comment</a>
                        </div>
                        <hr>
                        <% jobs.comments.forEach(function(comment){ %>
                            <div class="row">
                                <div class="col-md-12">
                                    <strong><%= comment.jobAuthor.username %></strong>
                                    <span class="pull-right">10 days ago</span>
                                    <p>
                                        <%= comment.text %>
                                    </p>
                                <% if(currentUser && comment.jobAuthor.id.equals(currentUser._id)){ %>
                                    <a class="btn btn-xs btn-warning" 
                                        href="/jobs/<%= jobs._id %>/comments/<%= comment._id %>/edit">
                                        Edit
                                    </a>
                                    <form id="delete-form" action="/jobs/<%= jobs._id %>/comments/<%= comment._id %>?_method=DELETE" method="POST">
                                        <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                                    </form>
                                <% } %>
                                </div>
                            </div>
                        <% }) %>    
                    </div>
                </div>
            </div>
        </div>
    </div>-->
</div>    

<% include ../partials/footer %>